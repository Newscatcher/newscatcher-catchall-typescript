## 1.2.0 - 2026-03-16
* The SDK now includes a `fetch()` method on the main client for making requests to endpoints not yet supported in the SDK. This method uses the SDK's configured authentication, retry logic, and other settings while providing a familiar fetch-like interface.

